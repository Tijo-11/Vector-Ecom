import { useState } from "react";
export default function ColourSize() {
  const [colourValue, setColourValue] = useState("No Colour");
  return <div></div>;
}

// Split it into components after completing
